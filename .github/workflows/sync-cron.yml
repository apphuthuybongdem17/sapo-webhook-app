name: Sync Inventory Cron

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Sapo inventory sync
        run: |
          curl -sfS -X POST \
            -H "Authorization: Bearer f604680fdf364f8b91a0b66b71685704" \
            "https://sapo-webhook-app.vercel.app/api/cron/sync-inventory"
